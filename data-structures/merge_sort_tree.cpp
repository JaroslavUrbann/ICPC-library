
/*
leaves form the array and node [l,r] has all elements in [l,r] sorted.
you can also just re-label the array so that all elements are distinct,
sort the indices by a_i and build a wavelet tree on top of them
*/
